import React from 'react'
import styles from './options-section.module.scss'

type OwnProps = {
}

export const OptionsSection: React.FC<OwnProps> = () => {

    return (
        <section className={ styles.authSection }>
            <div className={styles.authForm}>
                <h2>Опции преопции</h2>
            </div>
                </section>
                )
            }
